<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sz.jjj">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--允许程序连接到已配对的蓝牙设备。-->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!--允许程序发现和配对蓝牙设备。-->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MarioTheme.Day">
        <!--<activity android:name=".activity.MeituanBottomNavActivity">-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MeituanBottomNavActivity" />
        <activity android:name=".activity.EmptyRecyclerviewActivity" />
        <activity android:name=".activity.VideoViewActivity" />
        <activity android:name=".activity.WebViewActivity" />
        <activity android:name=".activity.ListernNotificationActivity" />
        <activity android:name=".activity.DataBaseActivity" />
        <activity
            android:name=".activity.Rotate3dAnimActivity"
            android:theme="@style/MarioTheme.Day" />
        <activity
            android:name=".activity.DayNightModeActivity"
            android:theme="@style/MarioTheme.Day" />
        <activity android:name=".activity.XmlParseActivity" />
        <activity android:name=".activity.UpdateApkActivity" />
        <activity android:name=".lockscreen.activity.LockScreenActivity" />
        <activity android:name=".lockscreen.activity.TimeActivity" />
        <activity android:name=".activity.SlidingTabActivity" />
        <activity android:name=".keyboard.SoftKeyboardActivity" />
        <activity android:name=".activity.FiveStarReViewActivity" />
        <activity android:name=".activity.BlueToothActivity" />
        <activity android:name=".activity.MusicButtonActivity" />



        <service
            android:name=".service.NotificationMonitorService"
            android:label="NotificationMonitor"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.MyAccessibilityService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/click_config" />
        </service>

    </application>

</manifest>